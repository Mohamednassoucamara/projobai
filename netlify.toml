[build]
  command = "npm run build"
  publish = "dist"

# SPA React Router : toutes les routes → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
